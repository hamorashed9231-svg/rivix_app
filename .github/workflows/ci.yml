name: CI / CD Pipeline - Quality & Infrastructure Automation

on:
  push:
    branches: [ main, master, develop ]
  pull_request:
    branches: [ main, master, develop ]

jobs:
  web-ci:
    name: Web Platform (Next.js) - Typecheck, Test & Build
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: restaurant-platform

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Node.js (v20)
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'
          cache-dependency-path: restaurant-platform/package-lock.json

      - name: Install Dependencies
        run: npm install

      - name: Generate Prisma Client
        run: npx prisma generate

      - name: TypeScript Type Check
        run: npx tsc --noEmit

      - name: Run Automated Vitest Unit & Integration Tests
        run: npm test

      - name: Verify Next.js Production Build
        run: npm run build
        env:
          DATABASE_URL: "file:./dev.db"

  mobile-ci:
    name: Mobile App (Expo / React Native) - Typecheck & Test
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: mobile

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Node.js (v20)
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'
          cache-dependency-path: mobile/package-lock.json

      - name: Install Dependencies
        run: npm install

      - name: TypeScript Type Check
        run: npx tsc --noEmit

      - name: Run Automated Vitest Unit Tests
        run: npm test
